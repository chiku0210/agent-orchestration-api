GROQ_API_KEY=
DATABASE_URL=

# Optional: minimum ms between `groq/compound` or `groq/compound-mini` calls (default ~2.3s) if you use them elsewhere.
# GROQ_COMPOUND_MIN_INTERVAL_MS=2300

# MarketPulse facet model (default: openai/gpt-oss-20b). Avoid `groq/compound` here — it can 413/429 on free tier.
# MARKETPULSE_FACET_MODEL=openai/gpt-oss-20b
